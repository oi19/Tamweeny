import React from "react";
import { StyleSheet, View, Text, Button, Image } from "react-native";

const productDetail = () => {
    return <Text> this is product Detail page</Text>
};

const styles = StyleSheet.create({
    
});


export default productDetail;